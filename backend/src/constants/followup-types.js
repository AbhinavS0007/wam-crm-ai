export const FOLLOWUP_TYPES = Object.freeze({
  CALL: 'call',
  MESSAGE: 'message',
  PROPOSAL: 'proposal',
  CUSTOM: 'custom',
});

export const FOLLOWUP_TYPE_VALUES = Object.freeze(Object.values(FOLLOWUP_TYPES));
