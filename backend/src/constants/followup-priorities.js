export const FOLLOWUP_PRIORITIES = Object.freeze({
  LOW: 'low',
  NORMAL: 'normal',
  HIGH: 'high',
  URGENT: 'urgent',
});

export const FOLLOWUP_PRIORITY_VALUES = Object.freeze(Object.values(FOLLOWUP_PRIORITIES));
