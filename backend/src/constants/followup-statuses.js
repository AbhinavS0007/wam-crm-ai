export const FOLLOWUP_STATUSES = Object.freeze({
  PENDING: 'pending',
  COMPLETED: 'completed',
  CANCELLED: 'cancelled',
  MISSED: 'missed',
});

export const FOLLOWUP_STATUS_VALUES = Object.freeze(Object.values(FOLLOWUP_STATUSES));
